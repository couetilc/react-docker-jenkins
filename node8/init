# -d <= run in background. Get info on all processes (including background) with command "docker ps"
# Print app output with command "docker logs <container_id>"
# Go inside container with command "docker exec -it <container_id> /bin/bash"
docker run \
    -p 1000:3000 \
    -d \
    couetilc/node-web-app
